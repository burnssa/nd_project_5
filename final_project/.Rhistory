```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- crimes[complete.cases(crimes),]
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv")
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
library(dplyr)
library(ggplot2)
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv")
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv")
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv")
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv', stringsAsFactors = FALSE)
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
```
summary(clean_crimes_df)
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv', stringsAsFactors = FALSE)
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Remove invalid latitude and longitude values}
crimes <- (subset(crimes$Lat, (crimes$Lat < 40 & crimes$Lat > 37)))
crimes <- subset(crimes$Lng, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv', stringsAsFactors = FALSE)
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Remove invalid latitude and longitude values}
crimes <- (subset(crimes$Lat, (crimes$Lat < 40 & crimes$Lat > 37)))
crimes <- subset(crimes$Lng, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(crimes)
head(crimes)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv', stringsAsFactors = FALSE)
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Remove invalid latitude and longitude values}
crimes <- subset(crimes$Lat, (crimes$Lat < 40 & crimes$Lat > 37))
crimes <- subset(crimes$Lng, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clearn_crimes_df)
head(clearn_crimes_df)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv', stringsAsFactors = FALSE)
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Remove invalid latitude and longitude values}
crimes <- subset(crimes$Lat, (crimes$Lat < 40 & crimes$Lat > 37))
crimes <- subset(crimes$Lng, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clean_crimes_df)
head(clean_crimes_df)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Remove invalid latitude and longitude values}
crimes <- subset(crimes$Lat, (crimes$Lat < 40 & crimes$Lat > 37))
crimes <- subset(crimes$Lng, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clean_crimes_df)
head(clean_crimes_df)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Remove invalid latitude and longitude values}
crimes <- subset(crimes$Lat, (crimes$Lat < 40 & crimes$Lat > 37))
crimes <- subset(crimes$Lng, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clean_crimes_df)
head(clean_crimes_df)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
summary(crimes)
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Remove invalid latitude and longitude values}
crimes$Lat <- subset(crimes$Lat, (crimes$Lat < 40 & crimes$Lat > 37))
crimes$Lng <- subset(crimes$Lng, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clean_crimes_df)
head(clean_crimes_df)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
summary(crimes)
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
library(dplyr)
```
```{r Remove invalid latitude and longitude values}
crimes$Lat = subset(crimes$Lat, (crimes$Lat < 40 & crimes$Lat > 37))
crimes$Lng = subset(crimes$Lng, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clean_crimes_df)
head(clean_crimes_df)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
summary(crimes)
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
library(dplyr)
```
```{r Remove invalid latitude and longitude values}
crimes$Lat - subset(crimes$Lat, (crimes$Lat < 40 & crimes$Lat > 37))
crimes$Lng <- subset(crimes$Lng, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clean_crimes_df)
head(clean_crimes_df)
#write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
library(dplyr)
```
```{r Remove invalid latitude and longitude values}
crimes <- subset(crimes$Lat, (crimes$Lat < 40 & crimes$Lat > 37))
crimes <- subset(crimes$Lng, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clean_crimes_df)
head(clean_crimes_df)
#write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
library(dplyr)
```
```{r Remove invalid latitude and longitude values}
subset(crimes$Lat, (crimes$Lat < 40 & crimes$Lat > 37))
subset(crimes$Lng, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clean_crimes_df)
head(clean_crimes_df)
#write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Remove invalid latitude and longitude values}
crimes <- subset(crimes, (crimes$Lat < 40 & crimes$Lat > 37))
crimes <- subset(crimes, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clean_crimes_df)
head(clean_crimes_df)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
summary(crimes)
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Remove invalid latitude and longitude values}
crimes <- subset(crimes, (crimes$Lat < 40 & crimes$Lat > 37))
crimes <- subset(crimes, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clean_crimes_df)
head(clean_crimes_df)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv", row.names=FALSE)
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Remove invalid latitude and longitude values}
crimes <- subset(crimes, (crimes$Lat < 40 & crimes$Lat > 37))
crimes <- subset(crimes, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clean_crimes_df)
head(clean_crimes_df)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv")
```
type(crimes$date_format)
typeof(crimes$date_format)
crimes$date_format[1] < '2015-07-01'
crimes$date_format[1]
subset(crimes, (as.numeric(crimes$date_format) < '2015-07-01'))
as.numeric(crimes$date_format[1]
)
subset(crimes, (as.numeric(crimes$date_format[1]) < as.numeric('2015-07-01'))
subset(crimes, (as.numeric(crimes$date_format[1]) < as.numeric('2015-07-01')))
subset(crimes, (as.numeric(crimes$date_format) < as.numeric('2015-07-01')))
as.numeric('2015-07-01')
as.numeric(as.character('2015-07-01'))
as.Date('2015-07-01')
crimes$date_format[1] < as.Date('2015-07-01')
as.Date(crimes$date_format[1]) < as.Date('2015-07-01')
as.Date(crimes$date_format[1]) < as.Date('2013-07-01')
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
```
```{r Remove records with invalid latitude and longitude values}
crimes <- subset(crimes, (crimes$Lat < 40 & crimes$Lat > 37))
crimes <- subset(crimes, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Remove records with invalid date values}
crimes <- subset(crimes, (as.Date(crimes$date_format) < as.Date('2015-08-01')))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
summary(clean_crimes_df)
head(clean_crimes_df)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv")
typeof(crimes$date_format)
as.Date(crimes$date_format[1]) < as.Date('2013-07-01')
```
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
install.packages("rjson")
library("rjson")
```
```{r Remove records with invalid latitude and longitude values}
crimes <- subset(crimes, (crimes$Lat < 40 & crimes$Lat > 37))
crimes <- subset(crimes, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Remove records with invalid date values}
crimes <- subset(crimes, (as.Date(crimes$date_format) < as.Date('2015-08-01')))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv")
```
```{r Add geojson file with neighborhoods}
neighborhood_data <- fromJSON(file=neighborhoods.geojson)
```
```{r Add geojson file with neighborhoods}
neighborhood_file <- 'neighborhoods.geojson'
neighborhood_data <- fromJSON(file=neighborhood_file)
```{r Add geojson file with neighborhoods}
#json_file <- "http://webonastick.com/uscl/feeds/uscl.json.txt"
neighborhood_data <- fromJSON(file='neighborhoods.geojson')
getwd()
neighborhood_data <- fromJSON(file='neighborhoods.geojson')
head(neighborhood_data)
library(jsonlite)
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
#install.packages("rjson")
#library("rjson")
library(jsonlite)
```
```{r Remove records with invalid latitude and longitude values}
crimes <- subset(crimes, (crimes$Lat < 40 & crimes$Lat > 37))
crimes <- subset(crimes, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Remove records with invalid date values}
crimes <- subset(crimes, (as.Date(crimes$date_format) < as.Date('2015-08-01')))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv")
```
```{r Add geojson file with neighborhoods}
#json_file <- "http://webonastick.com/uscl/feeds/uscl.json.txt"
getwd()
neighborhood_data <- fromJSON('neighborhoods.geojson', flatten=TRUE)
head(neighborhood_data)
```{r Setting up libraries (a)}
setwd('/Users/burnssa/sites/data_analyst_nanodegree/nd_project_5/final_project')
getwd()
crimes <- read.csv('oakland_crimes.csv')
summary(crimes) #Output suppressed for final report
head(crimes) #to illustrate the available fields, with example values
#Include libraries - all critical to the analysis
#library(dplyr)
#library(ggplot2)
#install.packages("rjson")
#library("rjson")
library(jsonlite)
```
```{r Remove records with invalid latitude and longitude values}
crimes <- subset(crimes, (crimes$Lat < 40 & crimes$Lat > 37))
crimes <- subset(crimes, (crimes$Lng > -123 & crimes$Lng < -122.1 ))
```
```{r Remove records with invalid date values}
crimes <- subset(crimes, (as.Date(crimes$date_format) < as.Date('2015-08-01')))
```
```{r Cleaning up crimes csv}
#Remove 'NA' rows
clean_crimes_df <- na.omit(crimes)
write.csv(clean_crimes_df, file = "oakland_crimes_clean.csv")
```
```{r Add geojson file with neighborhoods}
#json_file <- "http://webonastick.com/uscl/feeds/uscl.json.txt"
getwd()
neighborhood_data <- fromJSON('neighborhoods.geojson', flatten=FALSE)
head(neighborhood_data)
```
head(neighborhood_data)
detach("rjson")
detach(rjson)
detach("rjson", TRUE)
